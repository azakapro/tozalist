# Fail fast when the local Node version does not match "engines".
engine-strict=true
# Keep the workspace deterministic: workspace packages are linked, never fetched.
link-workspace-packages=true
prefer-workspace-packages=true
